[router]
enabled = false
default_prompt = "llm_static_analysis"

# Domain-based routing: each hunt domain maps to model_override (optional)
# Router uses cwe_to_hunt_domain() to map CWE IDs to these domains
[router.domains]
"injection" = {}
"xss" = {}
"auth" = {}
"authz_absence" = {}
"path_traversal" = {}
"crypto" = {}
"resource" = {}
"deserialization" = {}
"memory_safety" = {}